<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hi1237.hello.boot.spring5boot.mybatis.PdsMapper">
    <insert id="insertPds" parameterType="Pds">
           INSERT INTO pds (userid, title, contents, ipaddr)
            values (#{userid},#{title},#{contents},#{ipaddr})
    </insert>

    <select id="lastPdsPno">

<!--        select AUTO_INCREMENT -1 pno from information_schema.TABLES-->
<!--        where TABLE_NAME = 'pds'-->

        select last_insert_id() from pds

    </select>

<!--    <select id="selectPds" resultType="Pds">-->
<!--         select bno, title, userid, regdate, thumbs, views from Pds-->
<!--            order by bno desc limit #{stnum}, 25-->
<!--    </select>-->

<!--    <select id="selectOnePds" resultType="Pds">-->
<!--        select * from Pds where bno = #{bno}-->
<!--    </select>-->

<!--    &lt;!&ndash; 조회수 증가시키는 코드&ndash;&gt;-->

<!--    <update id="updateViewPds">-->
<!--        update Pds set views = views + 1 where bno = #{bno}-->
<!--    </update>-->

<!--    <select id="selectCountPds">-->
<!--        select ceil(count(bno) / 25) cntpg from Pds-->
<!--    </select>-->

<!--    &lt;!&ndash; 동적질의문으로 검색 기능 구현 &ndash;&gt;-->
<!--    &lt;!&ndash; 단, 컬럼명이나 테이블명에는 #{} 가 아닌 ${}를 사용한다. &ndash;&gt;-->
<!--    <select id="selectFindPds" parameterType="map" resultType="Pds">-->
<!--        &lt;!&ndash; 보내야 하는 변수가 1개가 아닌 3개이상이라 parameterType=Map으로 바꿔줌&ndash;&gt;-->
<!--        select bno, title, userid, regdate, thumbs, views from Pds-->

<!--        <if test="findtype != 'titcont'">-->
<!--        where ${findtype} like concat('%', #{findkey}, '%')-->
<!--        </if>-->
<!--        <if test="findtype == 'titcont'">-->
<!--        where title like concat('%', #{findkey}, '%')-->
<!--                            or contents like concat('%', #{findkey}, '%')-->
<!--        </if>-->

<!--        order by bno desc limit #{stnum}, 25-->


<!--    </select>-->

<!--    &lt;!&ndash; 동적질의문으로 검색 기능 구현 2 : 검색했을 때 나오는 페이지네이션 &ndash;&gt;-->
<!--    &lt;!&ndash; 단, 컬럼명이나 테이블명에는 #{} 가 아닌 ${}를 사용한다. &ndash;&gt;-->
<!--    <select id="countFindPds" parameterType="map">-->
<!--        select ceil(count(bno) / 25) cntfpg from Pds-->
<!--        &lt;!&ndash;검색결과 나올 때 어캐 나올지...&ndash;&gt;-->
<!--        <if test="findtype != 'titcont'">-->
<!--            where ${findtype} like concat('%', #{findkey}, '%')-->
<!--        </if>-->
<!--        <if test="findtype == 'titcont'">-->
<!--            where title like concat('%', #{findkey}, '%')-->
<!--            or contents like concat('%', #{findkey}, '%')-->
<!--        </if>-->

<!--    </select>-->

</mapper>